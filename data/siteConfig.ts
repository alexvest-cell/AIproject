// Site-wide feature flags.
// Set a flag to true to re-enable the corresponding section in navigation,
// footer, and sitemap. No other changes required.
export const SITE_CONFIG = {
    SHOW_REVIEWS:   false,
    SHOW_GUIDES:    false,
    SHOW_USE_CASES: false,
    SHOW_STACKS:    false,
    SHOW_NEWS:      false,
} as const;
